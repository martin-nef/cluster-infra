---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: valheim
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: valheim-auth
  url: ssh://git@github.com/martin-nef/valheim.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: valheim
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./deploy
  prune: true
  sourceRef:
    kind: GitRepository
    name: valheim
  decryption:
    provider: sops
    secretRef:
      name: valheim-sops-age
